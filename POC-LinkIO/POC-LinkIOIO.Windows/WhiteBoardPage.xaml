<Page
    x:Class="POC_LinkIO.WhiteBoardPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:POC_LinkIO"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d" SizeChanged="appSizeChanged" Loaded="PageLoaded">

    <StackPanel Orientation="Horizontal" Background="#FFEDEDED">
        <ScrollViewer VerticalScrollBarVisibility="Auto">
            <StackPanel Orientation="Vertical" HorizontalAlignment="Left" VerticalAlignment="Top" Width="150px" >
                <TextBlock HorizontalAlignment="Left" VerticalAlignment="Top" Foreground="Black" FontSize="20" Margin="10,10,10,10">Utilisateurs :</TextBlock>
                <TextBlock x:Name="Users" HorizontalAlignment="Left" VerticalAlignment="Top" Foreground="Black" FontSize="12" Margin="10,0,10,10"/>
            </StackPanel>
        </ScrollViewer>
        <StackPanel Orientation="Vertical" HorizontalAlignment="Left" VerticalAlignment="Top">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition/>
                    <ColumnDefinition/>
                </Grid.ColumnDefinitions>
                <StackPanel Orientation="Horizontal" HorizontalAlignment="Left" VerticalAlignment="Top">
                    <TextBlock>gdrf</TextBlock>
                </StackPanel>
                <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" VerticalAlignment="Top">
                    <Button x:Name="ButtonPhoto" Content="Prendre une photo" HorizontalAlignment="Right" VerticalAlignment="Top" FontSize="13.333" Foreground="Black" Click="ButtonPhotoClicked"/>
                    <Button x:Name="ButtonClear" Content="Tout supprimer" HorizontalAlignment="Right" VerticalAlignment="Top" FontSize="13.333" Foreground="Black" Click="ButtonClearClicked"/>
                </StackPanel>
            </Grid>
            <Canvas PointerPressed="pointerPressed"
                PointerMoved="pointerMoved"
                PointerReleased="pointerReleased"
                PointerExited="pointerExited"
                x:Name="Canvas"
                HorizontalAlignment="Left"
                VerticalAlignment="Top"
                Background="White"
                Width="auto"
                Height="auto"/>
        </StackPanel>
    </StackPanel>
</Page>
